apiVersion: v1
kind: Service
metadata:
  name: {{ include "javascript-lib.fullname" . }}
  labels:
    {{- include "javascript-lib.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "javascript-lib.selectorLabels" . | nindent 4 }}
