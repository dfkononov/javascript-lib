apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "javascript-lib.fullname" . }}-test-connection"
  labels:
    {{- include "javascript-lib.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "javascript-lib.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
